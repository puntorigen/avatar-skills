export interface StyleProps {
  text: string;
  durMs: number;
  width: number;
  height: number;
  fps: number;
  accentColor: string;
  textColor: string;
  fontFamily: string;
  subtitle?: string;
}
